# 用于运行数据库迁移的 Dockerfile
FROM node:18-alpine

WORKDIR /app

# 安装 OpenSSL（Prisma 需要）
RUN apk add --no-cache openssl libc6-compat

# 复制 package.json 和 prisma
COPY package.json package-lock.json* ./
COPY prisma ./prisma/

# 安装依赖
RUN npm ci

# 生成 Prisma Client
RUN npx prisma generate

# 运行迁移命令
CMD ["npx", "prisma", "migrate", "deploy"]
